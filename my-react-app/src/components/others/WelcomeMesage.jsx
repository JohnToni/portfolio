/*

export function WelcomeMessage({name, title}) {
    return (
        <>
            {title ? <h1 class="text-3xl font-bold underline">Hello {name || 'default'}!</h1> 
            : <p >Hello {name || 'default'}!</p>
            }
        </>
    )
}

*/